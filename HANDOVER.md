# HANDOVER

doverture の現在地のスナップショット。記憶ゼロのセッションがここから再開できる
ことを目的に書く。「なぜそう決めたか」は `DECISIONS.md`、「何をする約束か」は
`CLAUDE.md`。このファイルは**何が起きて、いま何が分かっていないか**。

## 現在地（2026-09-20）

**北海道全域センサスを実行中。** 設計は D1〜D8 で固まった。

- **母集団確定**：「国土地理院がbvmapを出している北海道」＝ z14セル **27,947**、
  194市区町村、14振興局（D8）
- **センサス実行中**：475,099タイル、実測65タイル/秒で**約2時間**。
  出力 `data/hokkaido-z14-census.csv`（途中再開可、既存 (x,y) は飛ばす）
- **生タイルをキャッシュ中**：`/Volumes/Migrate-2025-04/doverture-tilecache`。
  **フル調査（建物単位マッチング）は追加リクエストゼロで回せる**
- パイロットは**北広島市（01234、38セル）**。センサスの部分集合

## 次の一手（センサスと並行してよい）

**`docs/` を組む。** センサスは待ち時間なので競合しない。

1. Open MCT 4.3.1 の静的シェル（CDN、ビルドなし。`window.SharedWorker = undefined`）
2. `tabularmaps/do` のプラグインを vendor へ複製（複製元コミットを控える）。
   **最初のマイルストーンは「do のグリッドで北広島市だけに色が付く」**
3. z14 パネル（Vite + MapLibre GL JS 6 → `docs/spatialid/`、`base: /doverture/spatialid/`）
4. ローカル確認ポートは **8779** 固定

## 分かっていること（確定した主な事実）

- **bvmap `BldA` の z14 は使えない**。残存率 0.81%（北広島市と東広島で小数点以下4桁
  まで一致）。bvmap は z16、Overture は z14（D2/D8）
- **計数規則**：タイル内座標の重心が `[0, extent)` のものだけ。再投影不要（D1）
- **日本にも極東ロシアにも Microsoft/Google は来ていない**。日本の非OSM分は
  `doi:10.5281/zenodo.8174931`（東アジア建物フットプリントの学術データセット）のみ。
  tileset の絞り込みでないことはタンザニア/ナイジェリア/テキサス/ブラジル/
  スラウェシで確認済み（D6）
- **測量法**：N03 は「国土地理院長承認（複製）R 4JHf 430」を負い複製には承認が要る。
  **集計値のみ公開する限り手続きは不要**という読み（D5）

## 分かっていないこと（重要な順）

1. **密度による逆転の実像**。市街地では Overture/bvmap = 0.64、北海道全体の
   無作為標本では 1.24。センサスが出たら最初に見るのはここ（D8）
2. 両ソースの実効鮮度。bvmap 2026-07-01、Overture は `planetiler:buildtime` と
   `sources.version` が矛盾（D4）
3. bvmap と N03 は原典が同じ「数値地図（国土基本情報）」なのに、GSI は bvmap を
   「基本測量成果ではない」と言う。この食い違いの意味（D5）
4. 北方領土で bvmap が建物を出しているか（AdmArea はあるが BldA は未確認）
5. 雲量データの入手方法（RQ の派生仮説）

## 環境メモ（slate）

- Python 3.10.11 + `uv`。依存は `uv run --with ...` で都度解決
- タイルキャッシュは **Migrate-2025-04**（ブートディスクに置かない）
- ローカル確認ポート **8779**
- `/Volumes/Migrate-2025-04/github/doverture`、remote `git@github.com:dwg7/doverture`

## 作法

- 外部リンクは載せる前に必ず fetch して確認する
- **TileJSON のメタデータを前提にしない**。実際にそのズームのタイルを見る（D3）
- 「綺麗すぎる数字」は tileset 側の絞り込みを疑い、対照地点で確かめる（D6）
- 集計スクリプトを書き直したら、前の実装の既知の数字と差分照合する（D2の教訓）
