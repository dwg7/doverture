# HANDOVER

doverture の現在地のスナップショット。記憶ゼロのセッションがここから再開できる
ことを目的に書く。「なぜそう決めたか」は `DECISIONS.md`、「何をする約束か」は
`CLAUDE.md`。このファイルは**何が起きて、いま何が分かっていないか**。

## 現在地（2026-09-20）

**北海道全域のセンサス完了。ダッシュボードと地図パネルが両方動いている。**

- **センサス完了**：z14 陸域セル **27,947**、115分。`data/hokkaido-z14-census.csv`
- **タイル**：`docs/data/doverture-cells.pmtiles`（2.1 MB、z4–12、3,197枚）。
  `scripts/build_tiles.sh` で再生成
- **Open MCT ダッシュボード**：`docs/`（tabularmaps/do の市区町村パネル）
- **z14 地図パネル**：`docs/spatialid/`（Vite + MapLibre GL JS 6 + PMTiles）。
  **描画を実機で確認済み**（2026-09-20、`描画 24 件（画面内）`）

```bash
./scripts/serve.py          # 8779、no-store + Range 対応
cd spatialid && npm test    # 17件、ブラウザ不要
```

## 全道の数字

| | 件数 |
|---|---:|
| bvmap 建物 | 3,137,284 |
| Overture 建物 | 3,029,763（比 **0.966**） |
| 　OSM 由来 | 2,705,306（89.3%） |
| 　東アジア学術 | 96,427（3.2%） |
| 　Microsoft ほか | 228,030（7.5%） |
| 建物ゼロのセル | 14,390（51.5%） |

全道 0.966 は**市街地の 0.64 と農村の 2.24 が打ち消し合った結果**で、平均値そのものより
「打ち消し合っている」という事実の方が中身がある。

## 次の一手

1. **`stars.optgeo.org` への PMTiles 設置**（Hidenori と stars 側に相談してから。
   ファイルは `docs/data/doverture-cells.pmtiles`、名前の案は `doverture_cells`）
2. **全道の分布を読む**。市街地／農村で向きが変わる現象を、194市区町村で通して見る
3. **外れ値の自治体を空中写真で当たる**（D11/D12 の検証ループ）
4. ダッシュボードの「見せ方」の残り（生件数の桁の歪み、指標の並び、`progress` の扱い）

## 分かっていること（主な確定事項）

- **bvmap `BldA` の z14 は使えない**。残存率 0.81%（D2/D8）
- **bvmap の小建物の記録率が場所で崩れる**。50m²未満の割合が市街地 42.0% →
  農村 6.9%（D11）。原典の測量粒度が場所で変わる疑い（未検証）
- **Google Open Buildings は日本・極東ロシアに一切無い。Microsoft は北海道に
  7.5% ある**（D6→D9 で訂正）
- **共和町の Microsoft 検出は、層化16点中15点が建物でなかった**（D12）
- **測量法**：集計値のみ公開する限り手続きは不要という読み（D5）

## 分かっていないこと

1. 密度による逆転の実像（全道データで読む）
2. bvmap の小建物記録率が場所で崩れる理由（都市計画区域の内外？ 未検証）
3. 両ソースの実効鮮度（Overture のメタデータが矛盾、D4）
4. bvmap と N03 が同じ原典なのに「基本測量成果ではない」とされる件の意味（D5）
5. `doi:10.5281/zenodo.8174931` の詳細（東アジア建物フットプリントの学術データ）

## 作法（痛い目を見て増えたもの）

- **開発サーバーのアクセスログを潰さない。** ブラウザが見えない側には唯一の客観的証拠
  （D18。有効化前は6往復、以後は毎回1往復で原因に到達している）
- **TileJSON のメタデータを前提にしない**。実際にそのズームのタイルを見る（D3）
- **「綺麗すぎる数字」は絞り込みを疑い、対照地点で確かめる**（D6→D9）
- **式は実データの属性で評価する。** 合成データでのテストは緑のまま嘘をつく（D20）
- **検査は失敗の形に合わせて一般化する**（ワーカー1個の存在確認では依存の404を逃した、D19）
- 集計スクリプトを書き直したら、前の実装の既知の数字と差分照合する（D2の教訓）
