# HANDOVER

doverture の現在地のスナップショット。記憶ゼロのセッションがここから再開できる
ことを目的に書く。「なぜそう決めたか」は `DECISIONS.md`、「何をする約束か」は
`CLAUDE.md`、「何が分かったか」は `README.md`。このファイルは**いま何が動いていて、
何が分かっていないか**。

## 現在地（2026-09-20）— 公開済み

**https://dwg7.unopengis.org/doverture/** が動いている（GitHub Pages、`main` の `/docs`、
HTTPS 強制）。PR #1（24コミット）はマージ済み。

```bash
./scripts/serve.py          # 手元で見る: http://localhost:8779/
cd spatialid && npm test    # 20件、ブラウザ不要
./scripts/build_tiles.sh    # センサスCSV → PMTiles（検証つき）
```

- **センサス完了**：z14 陸域セル **27,947**（115分）。`data/hokkaido-z14-census.csv`
- **タイル**：`docs/data/doverture-cells.pmtiles`（2.5 MB、z4–12、タイル1枚に8×8セル）
- **ダッシュボード**：Open MCT。`tabularmaps/do` の市区町村パネル＋空間IDマップ（動的 import）
- **地図パネル**：z16 以上で建物の輪郭（bvmap＝アクア実線 / Overture OSM＝赤実線 /
  Overture 非OSM＝**赤点線**）

**公開時に実測済み**：Range は効いている（206、離れた3箇所とも75〜78ms、
`pmtiles` 越しに札幌 z10 タイル 7,938 バイトを取得）。`docs/vendor/` も配信される
（Jekyll の既定 exclude は効かなかった）。

## 次にやると良いこと

1. **Microsoft 優勢の区画を、共和町以外でも空中写真で当たる。** 誤検出率が共和町固有か
   全道的かで、D21 の表の読み方が変わる。輪郭レイヤーがあるので地図上で回せる
2. **PMTiles over Pages の安定性を観測する**（D27）。失敗すれば赤い帯が出るので、
   頻度と出方を記録する。ひどいようなら stars へ移す
3. ダッシュボードの残り：生件数の桁の歪み（437〜30,485）、指標の並び、
   `progress` 指標はセンサス完了後は無意味

## 分かっていること（主な確定事項、詳細は README と DECISIONS）

- **全道 0.966 は打ち消し合いの見かけ。** 密度で層化すると 2.33（最も疎）→ 0.85（市街地）
- **疎な区画の Overture 優勢はほぼ Microsoft 分**。OSM だけでは bvmap を超えない
- **共和町の Microsoft 検出は16点中15点が建物でなかった**（D12）
- **Google Open Buildings は日本にも極東ロシアにも1件も無い**（D6→D9で Microsoft は訂正）
- **北方領土は別機構**：Microsoft ゼロ、OSM だけで bvmap を1.75倍上回る
- **bvmap は z16 でしか完全でない**（z14 は 4.5%）。集計も表示もこれに合わせてある
- **測量法**：集計値のみ公開する限り手続きは不要という読み（D5）

## 分かっていないこと

1. Microsoft の誤検出率は共和町固有か、全道的か（いちばん効く未解決点）
2. bvmap の小建物記録率が場所で崩れる理由（都市計画区域の内外？ 未検証）
3. 両ソースの実効鮮度（Overture のメタデータが矛盾、D4）
4. bvmap と N03 が同じ原典なのに「基本測量成果ではない」とされる件の意味（D5）
5. `https://fgd.gsi.go.jp/download/` は DNS が解決せず未確認（CLAUDE.md に注記済み）

## 作法（痛い目を見て増えたもの）

- **開発サーバーのアクセスログを潰さない。** ブラウザが見えない側には唯一の客観的証拠
  （D18。有効化前は6往復、以後は毎回1往復で原因に到達した）
- **式は実データの属性で評価する。** 合成データでのテストは緑のまま嘘をつく（D20）
- **検査は失敗の形に合わせて一般化する**（ワーカー1個の確認では依存の404を逃した、D19）
- **不変条件は結果ではなく、それを保証している構造に置く**（D24）
- **TileJSON のメタデータを前提にしない**。実際にそのズームのタイルを見る（D3）
- **「綺麗すぎる数字」は絞り込みを疑い、対照地点で確かめる**（D6→D9）
- 集計スクリプトを書き直したら、前の実装の既知の数字と差分照合する（D2）

これらのうち一般化できるものは [dwg7/cafebabe#2](https://github.com/dwg7/cafebabe/pull/2)
に寄稿してマージ済み。
