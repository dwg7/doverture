# DECISIONS

doverture の設計判断を、決めた順に追記していく append-only の記録。
後の項目が前の項目を**訂正する**ことがある（書き換えず、新しい項目として追記する）。

この記録は意図的に軽い。doverture は探索的なデータ分析であって、不可逆な大容量
成果物を守るプロジェクトではない。決定そのものより「試した仮説と、外れた結果」
の方が価値があるので、そちらは `HANDOVER.md` 側に厚く書く。

---

## D1 — z14 が唯一の共通ズーム。セル・計数規則・出力先が芋づるで決まった（2026-09-20）

### 発端

CLAUDE.md は空間IDセルの粒度も、座標系も、`tabularmaps/do` の入力形式も
「未解決」としていた。両エンドポイントの TileJSON を実測したところ、粒度は
選ぶものではなく**制約として決まっている**ことが分かり、そこから残りが連鎖的に
決まった。

### 実測した事実（2026-09-20、TileJSON を直接取得）

| レイヤー | minzoom | maxzoom |
|---|---|---|
| `bvmap` / `BldA` | **14** | 16 |
| `overture_buildings` / `building` | 4 | **14** |

**両者が重なるズームは z14 ただ一つ。** 併せて `overture_buildings` には
`building_part` レイヤーも存在し、`building` は `sources`（String）と
`@geometry_source`（String）を持つ（文字列の中身は未実測）。

### 決めたこと

1. **セル = z14 タイルそのもの**（空間ID ZFXY の f=0）。量子化コードも空間ID
   ライブラリも不要。`(14, x, y)` がセルID。
2. **計数規則 = タイル内座標系での重心が `[0, extent)` に入るものだけを数える。**
   MVT はタイル境界でジオメトリをクリップし buffer ぶん隣へはみ出すので、境界を
   またぐ建物は素朴に数えると二重計上される。重心規則なら各建物をちょうど1セルに
   決定的に割り当てられ、再投影も幾何演算も要らず整数のまま完結する。
3. **DuckDB の `ST_Read` で `{z}/{x}/{y}` を直接クエリする当初案は撤回。**
   `ST_Read` は GDAL 経由で、リモートのタイルピラミッドをそのまま読む口ではない。
   タイルは HTTP で取得して MVT をデコードし、DuckDB は「セル×ソース×件数」
   テーブル側の集計・ロールアップに使う。
4. **出力は データ第一級 / ビュー差し替え可能**。`data/` に件数テーブル、
   `docs/` に GitHub Pages。詳細は CLAUDE.md「アウトプットと公開」。
5. **`docs/` は Open MCT 4.3.1 の CDN 読み込み・ビルドなし構成**（`m3xx-fleet` /
   `sas0` / `kikimimi` / `tabularmaps/do` と同じ）。**z14 マップだけ Vite +
   MapLibre GL JS 6 で `docs/spatialid/` へ出す**（`emptyOutDir` が `docs/` の
   Open MCT 側を消すため、直下には出さない）。
6. **市区町村パネルは `tabularmaps/do` のプラグインを採択**（vendor へ複製、
   複製元コミットを控える）。doverture が用意するのは
   `{label, unit, asOf, values: {"01100": 12.3}}` だけ。北方領土6村の扱いと表記は
   do 側の既定にそのまま従い、独自判断を持ち込まない。

### まだ検証していない、この全部を支えている前提

**bvmap `BldA` の z14 が間引かれていないこと。** z14 は bvmap 側にとって最も粗い
端なので、建物が間引かれている可能性がある。間引かれていれば z14 での件数比較は
bvmap 側の過少計数になり、比較そのものが成立しない。**東広島で「z14 の件数」と
「その下の z16 タイル群の合計」を突き合わせるのが、次の一手。** ここが崩れたら
手法を組み直す。

### 参考にした slate 側の教訓

`mapterhorn-japan-bridge` D176 — 検証済みのプロトタイプを「きれいな」本番コードに
書き直したら同じ種類のバグが再発し、構造的な検証（`pmtiles verify`）は両方とも
通した。捕まえたのは**旧実装の既知の数字との差分照合**だけだった。集計スクリプトを
書き直すときは、必ず前の実装の数字と突き合わせる。

---

## D2 — D1 の前提は外れた。bvmap は z16 で読むしかない（2026-09-20）

### 何が起きたか

D1 は「bvmap `BldA` の z14 が間引かれていないこと」を全体を支える未検証の前提として
挙げ、東広島で確かめることにしていた。**確かめたら、外れた。**

東広島・西条周辺の同一 z14 セル（`14/14233/6522`）で実測：

| | 件数 | 建物面積の中央値 |
|---|---|---|
| bvmap `BldA` @ z14 | **21** | 3,374 m² |
| bvmap `BldA` @ z15（子1枚） | 38 | 811 m² |
| bvmap `BldA` @ z16（子16枚合計） | **2,580** | 86 m² |
| Overture `building` @ z14 | 1,809 | 102 m² |

z14 に残っているのは **0.8%**。しかも残り方が「ランダムに間引かれた」のではなく
**大きい建物だけが残っている**（z14 の最小が 140 m²、z16 の最小は 1 m²）。
`vt_code` の構成比は z14 と z16 でほぼ同じなので、地物種別によるフィルタではなく
**面積によるフィルタ**。tippecanoe の tiny-polygon reduction の挙動と整合する
（TileJSON の `generator_options` に `-pf -pk` はあるが `-pt` はない）。

### なぜ「大きい建物だけ z14 で比べる」という安い逃げ道が使えないか

面積しきい値ごとの件数を見ると：

| 閾値 | Overture z14 | bvmap z14 | bvmap z16合計 |
|---|---|---|---|
| ≥1000 m² | 34 | 21 | 31 |
| ≥2000 m² | 21 | **13** | 14 |

bvmap z14 は全21件のうち13件が 2000 m² 以上だが、同じ範囲を z16 で見ると
2000 m² 以上は14件しかない。**z14 の面積は簡略化によって膨らんでいる**——
単に絞り込まれているのではなく、幾何そのものが歪んでいる。閾値を揃えても
同じ母集団にはならないので、この逃げ道は塞がっている。

### 決めたこと

1. **セルは z14 のまま**（分析の単位としては据え置き）。D1 の1〜2（セル定義・
   重心による計数規則）は生きている。
2. **bvmap は z16 で採り、z14 セルへ畳む。** Overture は z14（自身の maxzoom、
   フル詳細）のまま。bvmap 側だけ 1 セルあたり 16 タイルになる。
3. D1 の 3〜6（DuckDB の役割、出力構成、`docs/`、`tabularmaps/do` 採択）は
   そのまま有効。

### この訂正で分かった、むしろ良い報せ

Overture z14（n=1,809、中央値 102 m²、p90 315 m²）と bvmap z16（n=2,580、
中央値 86 m²、p90 330 m²）は**面積分布がよく一致している**。つまり正しい
ズームさえ選べば、両者は同じ種類の母集団であり、**件数の比較には意味がある**。
この1セルでの比は Overture / bvmap = **0.70** ——doverture の最初の実数。

### コストへの影響（未決）

北海道全域を全数で採ると、z14 陸域セルを概算3万として
**Overture 3万 + bvmap 48万 ≒ 51万リクエスト**。`stars.optgeo.org` は他者の
インフラなので、この規模の投げ方は**実行前に判断を仰ぐ**。市町村ごとの層化
サンプリング（例: 各20セル × 185 = 3,700 セル ≒ 6.3万リクエスト）なら
一桁以上安く、探索としては十分に成立する。東広島パイロット
（矩形約380セル ≒ 6,400リクエスト）は CLAUDE.md のスコープ内なので先に実行した。

### 教訓

D1 は「未検証の前提」を明示的に書き、それを他の何よりも先に潰すと決めていた。
その通りにしたら1時間で外れた。**前提だと分かっているものを前提のまま進めない**
——`mapterhorn-japan-bridge` の不変条件 #6（文書が自信満々に逆を書いていた）と
同じ話を、こちらは事故の前に回収できた。

---

## D3 — bvmap の TileJSON の minzoom/maxzoom は連続性を保証しない（2026-09-20）

北海道の陸域セルを数えるため、`AdmArea`（行政区画、TileJSON上は minzoom4〜maxzoom16）を
z12 で採って陸マスクにしようとしたら、**7,659枚すべてで `AdmArea` が存在しなかった**。

札幌を含むタイルを各ズームで辿って確認：

| z | 4-7 | 8 | 9 | 10 | 11 | 12 | 13 | 14-16 |
|---|---|---|---|---|---|---|---|---|
| `AdmArea` | **あり** | なし | なし | なし | なし | なし | なし | **あり** |

**z8〜z13 に穴がある。** TileJSON の `minzoom:4, maxzoom:16` は嘘ではないが、
tile-join で束ねた4バンドル（`tiles-5000k` z4-7 / `tiles-1000k` z8-10 /
`tiles-200k` z11-13 / `tiles-25k` z14-16）の**和集合**であって、間が連続して
いることは意味しない。D2 の「z14 は間引かれている」と同じ class の罠——
**TileJSON のメタデータは、実際にそのズームのタイルを見るまで前提にしない。**

### 陸マスクの作り直し（方針変更）

z8-13 に使える行政区画がない以上、bvmap 内で安価な陸マスクは作れない
（`RdCL`・`Cntr` は道のない土地・平坦地で欠けるので、**建物のある疎な区画を
系統的に取りこぼす**——まさに見たい現象を消す偏りになる）。

代わりに、**市区町村界データを先に決めて、それを陸マスク兼ロールアップキーに
兼用する**。ロールアップ用にどのみち必要なので、二つの未決事項が一度に片付く：

1. 市区町村界データ（国土数値情報N03 / e-Stat / Overture `divisions`）を選ぶ
2. 北海道の矩形内の各 z14 セルを、所属市区町村コードへ割り当てる
3. 割り当てが付いたセルの集合 = センサスの対象。ロールアップキーは同時に手に入る

セル数の実測はこの後になるため、**センサスの総コストはまだ確定していない**。

---

## D4 — stars のカタログを見たら、未決事項の大半が既にそこにあった（2026-09-20）

`https://stars.optgeo.org/catalog`（martin の catalog エンドポイント）に **43 ソース**。
CLAUDE.md が「未選定」「要調査」としていたものが、ほとんど既に載っていた。

- **`pmtiles_ksj_n03_hkd`** — 国土数値情報 行政区域（北海道）N03 2023、z0-12、
  bounds が北海道そのもの。フィールドは `code`（5桁市区町村コード）・`municipality`・
  `pref`・`subprefecture`・`county_or_city`。**「AdmArea ならクラウドネイティブ、
  N03 なら非クラウドネイティブ」という対立は存在しなかった**——N03 も stars にある
- `overture_divisions` — Overture の divisions（z0-12）。N03 の代替候補
- `openstreetmap_jp_planet` — OpenMapTiles スキーマ、`building` は z13-14
- `seamlessphoto512` / `kitaphoto17` / `japan-seamless-aerial-z18` — 写真の選択肢

### 決めたこと

**陸マスクと市区町村キーは `pmtiles_ksj_n03_hkd` を z12 で1回なめて両方得る。**
z14 セルは z12 タイル内の 4×4 部分格子なので、タイル内座標のまま割り当てられる
（D1 の計数規則と同じ発想）。`AdmArea@z14` も陸マスクとしては実際に機能することを
確認した（札幌・無人山地・海岸・離島・北方領土すべてで存在、外洋は204）が、
**`AdmArea` には市区町村コードがない**ので、キーには使えない。N03 なら両方が一度に済む。

### 実測（z12 タイル 7,370 枚、101秒）

- **z14 陸域セル：27,947**
- 市区町村：**194**（179市町村 − 札幌市1 + 札幌10区 + 北方領土6村 = 194 で符合）
- 振興局：14（9総合振興局 + 5振興局、正しい）
- **センサス総タイル数：475,099**（bvmap z16 447,152 + Overture z14 27,947）
- 41 タイル/秒で **約3.2時間**

出力：`data/hokkaido-z14-cells.json`

### OSM単体は「Overture の `sources` フィルタ」を採る（未決事項の解決）

`openstreetmap_jp_planet` を素の OSM として使う案は**取り下げる**。東広島の同一セルで
この tileset の `building` は **80件**だったが、Overture の同じセルは
`@geometry_source = OpenStreetMap` が **1,806件**。OSM が疎なのではなく、
**この tileset 側が z14 で間引かれているか、スナップショットが古い**（D2 と同じ罠）。
Overture の `sources` / `@geometry_source` を読む方が正確で、しかも追加リクエストが
ゼロ。

### 日本の Overture はほぼ OSM だった

`@geometry_source` の内訳：東広島 OpenStreetMap **92.2%** / `doi:10.5281/zenodo.8174931`
7.8%、札幌 **95.0%** / 5.0%。**日本では Microsoft/Google の AI 建物はほぼ効いておらず、
非OSM分は単一の Zenodo DOI データセット**。RQ3（AI生成データ単体）の立て方は、
この事実に合わせて組み直す必要がある。

### 鮮度についての訂正（D2 の記述を修正）

D2 で「Overture は `planetiler:buildtime` 2026-03-28、bvmap は 2026-07-01 で3か月ずれ」
と書いたが、**`sources` の中身は `version: "2026-08-02"`**（OSM planet スナップショット）。
ビルド日時より後の日付が入っているので、**`planetiler:buildtime` は当てにならない**。
実効的な鮮度は 2026-08-02 前後とみられ、bvmap の 2026-07-01 とはむしろ近い。
確定はしていないが、「3か月ずれている」という先の記述は撤回する。

---

## D5 — 測量法まわりの一次情報（2026-09-20、Hidenori の問いを受けて）

N03 のデータセットページ（`nlftp.mlit.go.jp/ksj/gml/datalist/`）の記載を確認した。

**N03 2023（`pmtiles_ksj_n03_hkd` の中身）**
- 原典資料：国土地理院「数値地図（国土基本情報）」
- **「測量法に基づく国土地理院長承認（複製）R 4JHf 430」**
- **「本製品を複製する場合には、国土地理院の長の承認を得なければならない。」**
- 使用許諾条件：平成30年以降は適用する利用規約に基づく（オープンデータ）。
  2024年版は **CC BY 4.0** と明記
- **「※本データを二次利用する場合には、国土地理院に申請等必要な場合があります。」**
- サイト全体の利用規約（施行 2026-03-23）は **PDL1.0（公共データ利用規約 第1.0版）**、
  出典記載が要件

**読み取れること（法律の専門家の判断ではない）**

1. 測量法29条が要求しているのは**複製**についての承認。MLIT は R4JHf430 を取って
   N03 を作っており、**その先の複製には改めて承認が要る**と明記されている
2. doverture は「やらないこと」で既に**データそのもの・単純な複製物を公開しない**と
   決めている。**セル単位の件数は複製ではない**（1.8km メッシュの件数から建物の
   外周線は復元できない）ので、この線を守る限り承認手続きは要らない、という読み
3. ローカルへのタイルキャッシュは解析のための**使用**であって頒布ではない。
   測量法30条は基本測量成果を使用して**測量を実施する**場合の規定で、
   doverture は測量を実施しない
4. ただし MLIT 自身が「申請等必要な場合があります」と留保している。**境界線に
   近いことをやるなら、国土地理院に聞くのが正しい手順**（申請の窓口はある）

**気づいた論点（要追記）**：bvmap の原典も「数値地図（国土基本情報）」であり、
N03 と同じ。にもかかわらず GSI は bvmap を「基本測量成果と位置付けているものではない」
と明言している。矛盾とまでは言えない（同じ原典から、測量成果の枠外の派生物を
出すことはあり得る）が、**CLAUDE.md が「要再確認」としていた論点の核心はここ**。

**当面の運用**（これを守る限り追加手続きは不要という前提で進める）
- 公開するのは集計値のみ。ジオメトリ・地物単位のテーブルは公開しない
- 出典を明記する：国土地理院（bvmap の原典「数値地図（国土基本情報）」）、
  国土数値情報 N03（測量法に基づく国土地理院長承認（複製）R 4JHf 430 を含む）、
  Overture / OSM（ODbL）、smellman 氏、stars 運用チーム
- 迷ったら公開前に国土地理院へ照会する

---

## D6 — 日本にも極東ロシアにも、MicrosoftとGoogleは来ていなかった（2026-09-20）

Hidenori の問い「サハリンやクリルでは Microsoft や Google は動いているだろうか」を
実測した。`@geometry_source` の内訳：

| 地域 | 建物数 | OpenStreetMap | Google | Microsoft |
|---|---:|---:|---:|---:|
| 東広島（日本） | 1,959 | 92.2% | 0 | 0 |
| 札幌（日本） | 3,606 | 95.0% | 0 | 0 |
| **北方領土**（N03上は日本、120セル） | 496 | **100.0%** | 0 | 0 |
| **サハリン**（ユジノサハリンスク周辺） | 1,784 | **100.0%** | 0 | 0 |
| **サハリン北部**（オハ周辺） | 258 | **100.0%** | 0 | 0 |
| **中千島**（ロシア施政下、30セル） | 4 | **100.0%** | 0 | 0 |
| **カムチャツカ**（ペトロパブロフスク） | 2,047 | **100.0%** | 0 | 0 |

日本の非OSM分は Google でも Microsoft でもなく、すべて `doi:10.5281/zenodo.8174931`
= **"A first high-quality vector data of buildings in East Asian countries based on a
comprehensive large-scale mapping framework"**（東アジアの建物フットプリントの学術
データセット）だった。

### tileset の絞り込みではないことの確認（D2の教訓を適用）

100% という数字が綺麗すぎるので、MS/Google が確実に効いている地域を同じ tileset で
見た：

| 地域 | OpenStreetMap | Google Open Buildings | Microsoft ML Buildings |
|---|---:|---:|---:|
| タンザニア農村 | 50.5% | 39.6% | 10.0% |
| ナイジェリア農村 | 34.9% | **59.1%** | 6.0% |
| 米テキサス農村 | 60.6% | — | **39.4%** |
| ブラジル内陸 | 79.0% | 16.7% | 4.2% |
| インドネシア・スラウェシ | 24.3% | 47.8% | 27.9% |

**tileset は絞り込まれていない。** よって日本・極東ロシアでの不在は本物。

いちばん強い証拠は**中千島**：30セルで建物わずか4件。OSMが疎なせいでMSが
出てこない、という説明は成り立たない——空白そのものがMSの埋める対象のはずで、
それが埋まっていない。**OSMの密度ではなく、地域そのものが対象外**とみられる。

### RQ3 の書き換え

CLAUDE.md の RQ3 は「AI生成データ単体（Microsoft ML Buildings / Google Open
Buildings 等）」を前提にしていたが、**この地域でその2つは走っていない**。
代わりに走っているのは上記の東アジア学術データセット。RQ3 は死んでおらず、
**登る相手が違っていた**。北海道では非OSM分が全体の約18%（200セル標本）。

---

## D7 — センサスの実行とスキーマ（2026-09-20）

Hidenori より `stars.optgeo.org` は本人管理のため規模の遠慮は不要との判断。実行した。

- 対象：27,947セル / 475,099タイル。実測レート65タイル/秒（6並列）で**約2時間**
- 出力：`data/hokkaido-z14-census.csv`
- **生タイルを `/Volumes/Migrate-2025-04/doverture-tilecache` にキャッシュする。**
  ブートディスクには置かない（`mapterhorn-japan-bridge` のTMPDIR不変条件と同じ理由）。
  これにより**フル調査（建物単位マッチング）は追加リクエストゼロで回せる**
- 途中再開可（出力CSVに既にある (x,y) は飛ばす）

### スキーマを広く取った理由

再取得は2時間かかるが、デコード時に**ついでに計算できるものはコストゼロ**なので、
後から欲しくなりそうなものを最初から全部入れる：

`z, x, y, code, bvmap_n, bvmap_area_m2, bv_3101, bv_3102, bv_3103, bv_3111, bv_3112,
ov_n, ov_area_m2, ov_osm, ov_eab, ov_other`

面積を入れたのは密度・建物サイズ分布を後から見られるようにするため。`bv_31xx` は
bvmap の建物種別（普通建物/堅ろう建物ほか）の内訳。`ov_osm`/`ov_eab`/`ov_other` で
D6 の三者を分離する。

---

## D8 — 母集団を「国土地理院がbvmapを出している北海道」に確定。パイロットは北広島市へ（2026-09-20）

### 母集団の確定（Hidenori の判断）

**「国土地理院がbvmapを出している北海道」**。根拠は D6——周辺へ広げても
Google にも Microsoft にも会えない（サハリン・千島・カムチャツカすべて
100% OpenStreetMap）。**境界を外へ広げる動機がない**ので、ここで確定。

実体は N03（`pmtiles_ksj_n03_hkd`）で陸と判定された **z14セル27,947個**、
**194市区町村**、**14振興局**。北方領土を含めるのは `tabularmaps/do` の既定
（政府の慣行）に従う。

### パイロットの移動：東広島市 → 北広島市

**これは誤記の訂正ではなく、判断の変更。** 旧版のCLAUDE.mdは
「パイロット地域と本目的の対象地域が異なる点は意図的」と明記して、
東広島市（**広島県**34212）を選んでいた。母集団を確定した時点でその理由が
消えたので、**北広島市（北海道01234、石狩振興局、z14セル38個）**へ移す。

決め手は `tabularmaps/do` が北海道179市町村の16×16グリッドであること——
**道外の自治体はそもそも描けない**ので、東広島市ではダッシュボードの
市区町村パネルを一度もテストできない。ダッシュボードの最初のマイルストーンは
「do のグリッドで北広島市だけに色が付く」こと。

北広島市はセンサスの部分集合なので、**別途の採取は不要**。

### D2 を母集団の内側で取り直した

D2（bvmap z14 の間引き）は東広島＝道外での実測だったので、北広島市38セルで
取り直した：

| | 件数 | 中央 m² | p90 | 最小 |
|---|---:|---:|---:|---:|
| bvmap `BldA` @ z14 | 281 | 2,140 | 5,625 | 1,256 |
| bvmap `BldA` @ z16 合計 | 34,887 | 73 | 144 | 0.9 |
| Overture `building` @ z14 | 22,301 | 90 | 198 | 2.4 |

**残存率 0.0081** ——東広島の 0.0081 と小数点以下4桁まで一致。
**局所現象ではなく tippecanoe の系統的な性質**であることが確定した。
D2 の結論（bvmapはz16から採る）はそのまま有効で、根拠が母集団の内側へ移った。

### 副産物：密度によって優劣が逆転している

- 北広島市（市街地を含む38セル）：Overture / bvmap = **0.639**
- 東広島市（道外、340セル）：**0.63**
- 北海道全体の無作為200セル標本：**1.24**

**市街地では bvmap が多く、北海道全体（54%が建物ゼロの疎な土地を含む）では
Overture が多い。** 密度によって捉え方の優劣が逆転している可能性があり、
これは CLAUDE.md が期待していた「地域の居住の本質を照らす手がかり」そのもの。
センサスの完了を待って本格的に見る。

### 東広島のデータの扱い

`data/higashihiroshima-z14.csv`（340セル）は母集団の外なので `data/` から外した
（スクラッチパッドへ退避、140秒で再生成可能）。D2/D8 に数値は残してある。
